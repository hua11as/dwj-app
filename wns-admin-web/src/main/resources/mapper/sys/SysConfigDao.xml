<?xml version="1.0" encoding="UTF-8"?>  
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">    
<mapper namespace="com.lontyu.dwjadmin.dao.SysConfigDao">

	<!-- 根据key，更新value -->
	<update id="updateValueByKey" parameterType="map">
		update sys_config set param_value = #{paramValue} where param_key = #{paramKey}
	</update>

	<!-- 根据key，查询value -->
	<select id="queryByKey" parameterType="string" resultType="com.lontyu.dwjadmin.entity.SysConfigEntity">
		select * from sys_config where param_key = #{paramKey}
	</select>

	<select id="selectOdds" resultType="com.lontyu.dwjadmin.entity.SysConfigEntity">
		select id,param_key,param_value from sys_config where status=1 and id &lt; 7 order by id asc
	</select>

	<select id="selectNeedRecords" resultType="com.lontyu.dwjadmin.entity.SysConfigEntity">
		select id,param_key,param_value  from sys_config where status=1 and id &lt; 7
	</select>
</mapper>